defmodule ElixirScribe.MixGenerator.App.BuildResourceActionFilename.BuildResourceActionFilenameApp do

  # alias Mix.Phoenix.Context
  # alias  ElixirScribe.MixGenerator.AppApiContract.BuildResourceActionFilenameContract

  # @doc false
  # def build(%BuildResourceActionFilenameContract{} = contract) do
  #   resource =
  #     (contract.action in ElixirScribe.resource_plural_actions() && contract.context.schema.plural) || contract.context.schema.singular

  #   "#{contract.action}_" <> resource <> contract.suffix
  # end
end
